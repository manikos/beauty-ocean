language: python
python: 
  - "3.6"
  - "3.7"

env:
  - TOX_ENV=py36

matrix:
  fast_finish: true

# command to install dependencies, e.g. pip install -r requirements.txt --use-mirrors
install: pip install beauty-ocean[test]

# command to run tests using coverage, e.g. python setup.py test
script: tox -e $TOX_ENV

after_success:
  - codecov -e TOX_ENV
